<!-- eslint-disable -->
<template>
  <component     
    v-for="item in list"
    :key="item" :is="config[item]" :width="props.width" :height="props.height" />
</template>
<script setup lang="ts">
import { defineProps } from 'vue';
{{#each iconNameList }}
import {{component}} from './{{component}}.vue';
{{/each}}

const props = defineProps<{
  name:
  {{#each iconNameList }}
    | '{{name}}'
  {{/each}};
  width?: string | number;
  height?:string | number
}>();

const config = {
  {{#each iconNameList }}
  '{{name}}': {{component}},
  {{/each}}
};

const list = [  {{#each iconNameList }}
  '{{component}}',
  {{/each}}]
</script>